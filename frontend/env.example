# This file serves as an example for setting up your environment variables.
# Copy this file to `.env` and fill in your actual values.
# Do NOT commit your actual .env file to version control (e.g., Git)!

# --- Frontend API Configuration ---
# The base URL for your backend API.
# During local development, this might be http://localhost:8000.
# In a Docker Compose environment, this should point to the API service name,
# e.g., http://api:8000 (if 'api' is the service name in docker-compose.yml)
# In production, this will be your deployed API's public URL.
VITE_API_BASE_URL=http://localhost:8000